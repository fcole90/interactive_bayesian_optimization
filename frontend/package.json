{
  "name": "interactive_bayesian_optimisation_frontend",
  "version": "0.0.1",
  "main": "src/index.ts",
  "repository": "https://github.com/fcole90/interactive_bayesian_optimisation.git",
  "author": "fcole90 <fcole90@gmail.com>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "sync-jquery": "rm -rf src/3rd/jquery && mkdir -p src/3rd/jquery && cp -r node_modules/jquery/ src/3rd/jquery",
    "lint": "eslint .",
    "build": "yarn sync-jquery && tsc --build",
    "dev": "yarn sync-jquery && tsc --watch",
    "postinstall": "yarn sync-jquery"
  },
  "devDependencies": {
    "@types/jquery": "^3.5.22",
    "@typescript-eslint/eslint-plugin": "^6.7.5",
    "@typescript-eslint/parser": "^6.7.5",
    "eslint": "^8.51.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "jquery": "^3.7.1"
  }
}
